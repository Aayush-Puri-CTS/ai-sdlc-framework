{
  "name": "ai-sdlc-framework",
  "version": "1.0.0",
  "private": true,
  "description": "Shared invariant core for the AI-SDLC framework: config schema, validators, hooks, agent contracts, and templates vendored into consuming repositories.",
  "type": "module",
  "scripts": {
    "validate-config": "node scripts/validate-config.mjs",
    "scaffold": "node scripts/scaffold.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "js-yaml": "^4.1.0",
    "minimatch": "^10.0.0"
  }
}
