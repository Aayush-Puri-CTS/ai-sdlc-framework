<!--
  ADR/0000-template.md — vendored as-is into every consuming repo.
  Copy this file to ADR/NNNN-short-title.md for each Tier D change
  (AI-SDLC-FRAMEWORK-SPEC.md section 6). A Tier D task may not begin
  implementation until an ADR following this shape is approved.
-->

# NNNN. <<Short, specific title — not "database changes", but "switch
primary key strategy from UUID to ULID">>

**Status:** <<Proposed | Accepted | Superseded by ADR-NNNN>>
**Date:** <<YYYY-MM-DD>>
**Tier D trigger matched:** <<which entry in `tiers.D_triggers` (project.config.yml) this change matches>>

## Context

<<What is the problem or need driving this change? What constraints
apply? Link the originating task spec (`docs/specs/<feature>.md`) or
ticket if one exists.>>

## Decision

<<What are we doing, specifically? State it so a future reader
understands the change without needing this conversation's context.>>

## Alternatives Considered

<<What else was considered, and why was it not chosen? "Nothing else"
is a valid answer if the decision is a policy default (e.g. adopting a
pre-existing pattern rather than choosing among genuine options).>>

## Consequences

<<What becomes easier or harder as a result? What follow-up work, if any,
does this create? Note any hard rules in `CLAUDE.md` that this decision
interacts with.>>

## Approval

<<Name the human approver(s) required before implementation may begin.
An agent may draft this ADR, but Tier D means an agent may not implement
against it until a human has actually approved it here.>>
